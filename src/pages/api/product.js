export default function product_list(req, res) {
  res.status(200).json({message:"get all product"})

    const product_list = (req,res)=>{
      res.status(200).json({message:"get all product"})
    }
    module.exports=product_list
  }
  